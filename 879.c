#include <string.h>

int profitableSchemes(int n, int minProfit, int* group, int groupSize, int* profit, int profitSize){
    (void)n;
    (void)minProfit;
    (void)group;
    (void)groupSize;
    (void)profit;
    (void)profitSize;

    // TODO:
    // DP 2D: dp[people][p] = nº de esquemas usando 'people' pessoas e lucro p (capado em minProfit).

    return 0;
}
