#include <stdlib.h>

long long maxTaxiEarnings(int n, int** rides, int ridesSize, int* ridesColSize){
    (void)n;
    (void)rides;
    (void)ridesSize;
    (void)ridesColSize;

    // TODO:
    // 1. Transformar cada corrida em (start, end, profit).
    // 2. Ordenar por end.
    // 3. Aplicar DP para pegar subconjunto ótimo de corridas não sobrepostas.

    return 0; // valor placeholder
}